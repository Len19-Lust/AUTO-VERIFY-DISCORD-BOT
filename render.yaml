services:
  - type: worker
    name: discord-bot
    runtime: python
    envVars:
      - key: DISCORD_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
